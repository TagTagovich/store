<form method="post" action="{{ path('app_base_delete', {'id': base.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ base.id) }}">
    <button class="btn">Удалить</button>
</form>
