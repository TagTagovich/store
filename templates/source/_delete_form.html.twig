<form method="post" action="{{ path('app_source_delete', {'id': source.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ source.id) }}">
    <button class="btn">Удалить</button>
</form>
